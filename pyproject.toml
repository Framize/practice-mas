[project]
name = "practice-mas"
version = "0.1.0"
description = "A starting point for building your own multi-agent system"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "openai-agents>=0.22.0",
    "python-dotenv>=1.1.0",
]
